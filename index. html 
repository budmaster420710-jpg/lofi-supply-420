<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LOFI SUPPLY 420 | THE VAULT</title>
    <style>
        body { background: #0a0a0a; color: #e0e0e0; font-family: 'Courier New', Courier, monospace; display: flex; flex-direction: column; align-items: center; padding: 20px; }
        h1 { color: #00ff88; text-shadow: 2px 2px #000; border-bottom: 2px solid #333; padding-bottom: 10px; width: 100%; text-align: center; letter-spacing: 5px; }
        .vault-container { width: 100%; max-width: 600px; }
        .track-card { background: #161616; border: 1px solid #333; padding: 20px; margin-bottom: 20px; border-radius: 4px; border-left: 5px solid #00ff88; }
        .track-title { font-weight: bold; font-size: 1.1rem; color: #fff; display: block; margin-bottom: 10px; text-transform: uppercase; }
        audio { width: 100%; margin: 10px 0; filter: invert(100%) hue-rotate(90deg); }
        .button-group { display: flex; gap: 10px; margin-top: 10px; }
        .buy-btn { flex: 1; padding: 12px; border: none; border-radius: 2px; cursor: pointer; font-weight: bold; text-align: center; text-decoration: none; font-size: 0.8rem; transition: 0.2s; }
        .digital { background: #00ff88; color: #000; }
        .license { background: #333; color: #00ff88; border: 1px solid #00ff88; }
        .buy-btn:hover { background: #fff; color: #000; }
    </style>
</head>
<body>

    <h1>LOFI SUPPLY 420</h1>
    <p style="color: #666; margin-bottom: 30px;">// 16 HIGH QUALITY WAV TRACKS</p>

    <div class="vault-container">

        <script>
            const tracks = [
                "Deep Space", "Nature's Breath", "Bedroom Beats Loop", "Midnight City", 
                "Rainy Windows", "Sunset Chill", "Lo-Fi Dreams", "Cloud Nine", 
                "Street Lights", "Coffee Shop", "Echoes", "Vinyl Static", 
                "After Hours", "Quiet Mind", "Slow Motion", "Final Wave"
            ];

            const container = document.querySelector('.vault-container');

            tracks.forEach((track, index) => {
                const card = document.createElement('div');
                card.className = 'track-card';
                card.innerHTML = `
                    <span class="track-title">${(index + 1).toString().padStart(2, '0')}. ${track}</span>
                    <audio controls src="${track}.wav"></audio>
                    <div class="button-group">
                        <a href="#" class="buy-btn digital">BUY WAV ($2)</a>
                        <a href="#" class="buy-btn license">LICENSE ($25)</a>
                    </div>
                `;
                container.appendChild(card);
            });
        </script>
    </div>

</body>
</html>
